<?xml version="1.0" encoding="UTF-8"?>
<!--
  Offline declaration for COLLADA's optional formula payload. S3 does not
  interpret formula/kinematics content; the declaration prevents the COLLADA
  schema from resolving MathML over the network while retaining namespace
  validation for the referenced global element.
-->
<xs:schema xmlns:xs="http://www.w3.org/2001/XMLSchema"
           targetNamespace="http://www.w3.org/1998/Math/MathML"
           xmlns="http://www.w3.org/1998/Math/MathML"
           elementFormDefault="qualified">
  <xs:element name="math">
    <xs:complexType mixed="true">
      <xs:sequence>
        <xs:any minOccurs="0" maxOccurs="unbounded" processContents="skip"/>
      </xs:sequence>
      <xs:anyAttribute processContents="skip"/>
    </xs:complexType>
  </xs:element>
</xs:schema>
